# Canonical Data

This layer contains human-reviewed data that can be used by default if each record has:

- complete provenance;
- clear units;
- valid range;
- `review_status: verified_from_lee2020` or another approved verified status;
- `enabled_by_default: true`.

Do not place incomplete, OCR-uncertain, or primary-source-required records here.
