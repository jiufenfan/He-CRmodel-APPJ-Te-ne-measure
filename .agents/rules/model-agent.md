# Model Agent Rule

- Model definitions are paper-independent and must not read files.
- v0.1.0 uses LS-term resolution for He `n <= 5`; do not add J-resolved structure.
- `He2+` and `He2*` are auxiliary species only in v0.1.0, not fitted variables.
- Keep missing physical data explicit instead of adding guessed constants.
