# Test Agent Rule

- Tests must protect scientific constraints, not only code behavior.
- Include tests for provenance, unit consistency, unverified-data disabling, level completeness, target spectral-line mapping, and non-negative outputs.
- Do not weaken tests to accommodate missing data; represent missing data as expected fail-closed behavior.
